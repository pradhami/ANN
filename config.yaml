params:
  epochs: 30
  batch_size: 32
  no_classes: 10
  loss_function: sparse_categorical_crossentropy
  metrics: accuracy
  optimizer: SGD
  validation_datasize: 5000

artifacts: 
  artifacts_dtr: artifacts
  model_dir: model
  plots_dir: plotsdir
  checkpoint_dir: checkpoints

logs:
  logs_dir: logs_dir
  general_logs: general_logs
  tensorboard_logs: tensorboard_logs
